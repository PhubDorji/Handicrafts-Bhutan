/** @type {import('next').NextConfig} */

const nextConfig = {
  images: {
    domains: ['www.handicraftsbhutan.org'],
    // You can add more domains as needed:
    // domains: ['www.handicraftsbhutan.org', 'another-domain.com'],
  },
};
export default nextConfig;
